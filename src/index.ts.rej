diff a/src/index.ts b/src/index.ts	(rejected hunks)
@@ -8,10 +8,10 @@ import cors from 'cors';
 import { generateToken } from './utils/auth';
 import { storeTokens } from './repositories/tokens';
 import { User } from '@prisma/client';
-import { subscriptionController } from './controllers/subscribtion';
 import swaggerUi from 'swagger-ui-express';
 import { readFileSync } from 'fs';
 import { usersRouter } from './routes/users';
+import { subcriptionsRouter } from './routes/subscriptions';
 
 
 const app = express();
@@ -104,26 +104,7 @@ app.get(
 
 app.use('/users', usersRouter);
 
-app.get(
-  '/users/available-for-call',
-  passport.authenticate('bearer', { session: false }),
-  profileController.availableForCall
-);
-
-app.post(
-  '/users/subscribe',
-  passport.authenticate('bearer', { session: false }),
-  subscriptionController.create
-);
-
-/**
- * 
- */
-app.get(
-  '/users/:userId/subscriptions',
-  passport.authenticate('bearer', { session: false }),
-  subscriptionController.get
-);
+app.use('/sub', subcriptionsRouter);
 
 /**
  * 
